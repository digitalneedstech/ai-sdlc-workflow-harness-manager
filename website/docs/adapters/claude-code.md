---
title: Claude Code
description: Same pack. Skill under .claude/skills/run-workflow. Observability merges Claude settings.
---

Install: `pipeline-kit init --ide claude-code`.

Skill path: `.claude/skills/run-workflow/SKILL.md`.

Archify on Claude Code may land under `.claude/skills/archify/` depending on `gh skill` scope. Observability install merges fail-open collector entries for Claude Code settings — it does not replace your existing hooks.

Open the project directory that contains `.pipeline/` as the working folder for the CLI session.
