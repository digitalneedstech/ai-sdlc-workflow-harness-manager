# Feature plan template (superseded)

| Attribute | Value |
|-----------|--------|
| Type | Template |
| Audience | The specialist that writes the artifact |
| Adapt | Do not fill this file. Write `prd.md` from [prd-template.md](prd-template.md). |

Owned by **feature-development**. PM no longer writes `plan.md` as the
requirements artifact. The signed document is `features/{slug}/prd.md`.

If an older run still has `plan.md`, treat it as input while rewriting the
PRD. Do not create a new thin plan instead of a PRD.
