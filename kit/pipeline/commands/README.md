# Commands

| Attribute | Value |
|-----------|--------|
| Type | Handbook |
| Audience | Pack maintainers |
| Adapt | Add a file here only when a workflow needs a user-facing command that is not a skill or an agent brief. |

Reserved for slash-command or CLI wrappers that should travel with the pack
instead of an IDE folder. None ship yet.
